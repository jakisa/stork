#ifndef module_hpp
#define module_hpp

#include <functional>

namespace stork {
/*
	inline double my_nothing(double, double){
		return 0;
	}
	
	template<typename R, typename... Args>
	void add_function2(const std::function<R(Args...)>& f){
	}

	class module {
	private:
		template<typename R, typename... Args>
		void add_function(const std::function<R(Args...)>& f){
		}
	public:
		template<typename... Fs>
		module(Fs... fs) {
			//(add_function(std::function(fs)), ...);
			add_function2(std::function(&my_nothing));
		}
		
		void load(const char* path);
		bool try_load(const char* path);
		
		void reset();
		
	};
*/
}
#endif /* module_hpp */
